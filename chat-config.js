/* Chat con Claude Haiku — configurazione per l'app installata.
   Nell'anteprima dello strumento la chat usa l'aiuto integrato.
   Fuori (GitHub Pages, app installata) serve un piccolo proxy che
   custodisca la tua chiave API: NON mettere mai la chiave qui dentro.

   1. Pubblica il proxy (esempio Cloudflare Worker in proxy-example.js).
   2. Incolla il suo indirizzo qui sotto.
   3. Senza indirizzo, Pericle usa le risposte pronte per categoria. */

window.PERICLE_CHAT_ENDPOINT = "";
